name: Playwright Tests
on:
  push:
    branches: [ main, master ]
jobs:
  test: 
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: '14.x'
      run: npm ci
<<<<<<< HEAD
    - name: run test
      run: npm run test
    - name: Copy History
      if: always()
      run: cp -r $(pwd)/docs/history/. $(pwd)/allure-results/history
    - name: Generate Report
      if: always()
      run: npm run allure-generate # Name of the allure generate script
    - name: Upload Results
      uses: actions/upload-artifact@v1
      if: always()
      with:
          name: allure-results
          path: ./allure-results # Name of the folder with allure results                    
    - name: Publishing Allure artifact to Github Actions
      uses: actions/upload-artifact@v2
      if: always()
      with:
          name: allure-report
          path: allure-report        
    - name: Deploy report
      uses: JamesIves/github-pages-deploy-action@4.1.4
      if: always()
      with:
          branch: gh-pages # The branch the action should deploy to.
          folder: allure-report # The folder the action should deploy.
          target-folder: docs
    - name: Pushes docs to another repository
      uses: cpina/github-action-push-to-another-repository@v1.3
      if: always()
      env:
          API_TOKEN_GITHUB: ${{ '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' }} # Instruction where to get token is below
        with:
          source-directory: 'docs'
          destination-github-username: 'Valerii' # Your username
          destination-repository-name: 'gh-pages' # Name of your repo
          user-email: mandyka.valerii@gmail.com # Your email
          target-branch: main 


=======
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: npm run test
    - uses: actions/upload-artifact@v2
      if: always()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
    - name: Get Allure history
      uses: actions/checkout@v2
      if: always()
      continue-on-error: true
      with:
        ref: gh-pages
        path: gh-pages
    - name: Generate Report
      if: always()
      run: npm run allure-generate
    - name: Upload Results
      uses: actions/upload-artifact@v1
      if: always()
      with:
          name: allure-results
          path: ./allure-results
    - name: Test marketplace action
      uses: simple-elf/allure-report-action@master
      if: always()
      id: allure-report
      with:
        allure_results: build/allure-results
        gh_pages: gh-pages
        allure_report: allure-report
        allure_history: allure-history
    - name: Deploy report to Github Pages
      if: always()
      uses: peaceiris/actions-gh-pages@v2
      env:
        PERSONAL_TOKEN: ${{ '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' }}
        PUBLISH_BRANCH: gh-pages
        PUBLISH_DIR: allure-history
    
>>>>>>> da8a3ff9fc7ad6b2814343e21145f43375610423
